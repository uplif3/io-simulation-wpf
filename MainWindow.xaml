<Window x:Class="io_simulation_wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:io_simulation_wpf"
        xmlns:views="clr-namespace:io_simulation_wpf.Views"
        xmlns:vm="clr-namespace:io_simulation_wpf.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <views:IOView DataContext="{Binding IOVM}" Grid.Row="0" x:Name="IOPanel"/>
        <views:DebugView DataContext="{Binding DebugVM}" Grid.Row="1" MinHeight="180" MaxHeight="180" x:Name="DebugPanel"/>
    </Grid>
</Window>
